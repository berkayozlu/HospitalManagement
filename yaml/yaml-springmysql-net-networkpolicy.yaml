apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: yaml-springmysql-net
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/yaml-springmysql-net: "true"
  podSelector:
    matchLabels:
      io.kompose.network/yaml-springmysql-net: "true"
